<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <link rel="stylesheet" href="style.css">
  <title>Gig</title>
  <script>
  audiojs.events.ready(function() {
    var as = audiojs.createAll();
  });
</script>
</head>
<body>

  <div class="contents">
    <div class="top-content">
      <a href="#" class="link-logo">log-in</a>
    </div>

    <div class="search-content">
      <%= form_with(url: "#" , local: true, method: :get, class: "search-form") do |form| %>
        <%= form.text_field :keyword, placeholder: "投稿を検索する", class: "search-input" %>
        <%= form.submit "検索", class: "search-btn" %>
      <% end %>
    </div>

    <div class="middle-content">
      <div class="sound-tweet">
        <div class="user-name">
          
        </div>
        <div class="sound-explain">
        
        </div>
        <div class="favo"></div>
      </div>
      <div class="sound-file">
        <% @sound.each do |s| %>
          <div class="sound-content ">
            <p><%= s.user.name %>さんの投稿</p> 
            <p><%=link_to s.title, sound_path(s.id) %></p> 
            <p><%= s.explain %></p> 
            <%= audio_tag(s.file.url, autoplay: false, controls: true) %>
            <p><%=link_to "#{s.title}とセッション", sound_for_session_path(s.id) %></p> 
            <%#= link_to "ダウンロード" , s.file.url %>
          </div>
        <% end %>
      </div>
    </div>

  </div>

</body>
</html>